// @flow
import PropTypes from 'prop-types';

const IconProps = {
  title: PropTypes.string,
  size: PropTypes.number,
  icon: PropTypes.string,
  color: PropTypes.string,
};

const IconDefaultProps = {
  icon: '',
  size: 32,
};

export { IconProps, IconDefaultProps };
